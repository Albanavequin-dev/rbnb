<%= simple_form_for [@flat, @booking] do |f| %>
    <%= f.check_box :status %>
    <%= f.label :status, class: 'checkbox-inline wifi' %>
  <%= f.input :message_customer, placeholder: "Hello ! I'm on vacation with a friend..." %>
  <%= f.submit "Send answer", class: "btn btn-primary"  %>
<% end %>
